<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

<!--        <item name="menustyle"-->
<!--        <item name="android:actionMenuTextColor">@color/colorBackGray</item>-->
        <item name="android:windowBackground">@color/colorBackBlack</item>
        <item name="android:textColorPrimary">@color/colorNomalText</item>

    </style>

    <style name="ToolbarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="theme">@style/ToolbarTheme</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <!-- 设置 toolbar 溢出菜单的文字的颜色 -->
        <item name="android:textColor">@color/colorNomalText</item>
        <!-- 设置toolbar 溢出菜单的字体大小-->
<!--        <item name="android:textSize">25sp</item>-->
        <!-- 设置 显示在toobar上菜单文字的颜色 -->
        <item name="actionMenuTextColor">@color/colorNomalText</item>
        <item name="popupMenuBackground">@color/colorFrontBlack</item>
        <item name="actionBarItemBackground">@color/colorFrontBlack</item>
    </style>


       <style name="DialogActivityTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
        <item name="android:windowCloseOnTouchOutside">false</item>
        <!-- 重点，去掉标题 -->
        <item name="windowNoTitle">true</item>
    </style>
</resources>
